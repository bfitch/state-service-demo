import Ember from 'ember';

export default Ember.Route.extend({
  // model() {
  //   return {
  //     body: 'wow wow so neat'
  //   };
  // },
  //
  // setupController(controller, model) {
  //   this._super(controller, model);
  //
  //   this.controller.set('message', model);
  // }
});
